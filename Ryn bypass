-- =========================================================
-- TSB MULTI-TOOL SCRIPT (VERSION 2.7 - AUTO SKILL 1-2-3)
-- =========================================================

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local Lighting = game:GetService("Lighting")
local RunService = game:GetService("RunService")
local VirtualInputManager = game:GetService("VirtualInputManager")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

---------------------------------------------------------
-- 1. TẠO GUI (4 NÚT TÍNH NĂNG)
---------------------------------------------------------
if PlayerGui:FindFirstChild("TSB_Custom_Tools_Gui") then
    PlayerGui.TSB_Custom_Tools_Gui:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "TSB_Custom_Tools_Gui"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = PlayerGui

-- NHÃN HIỂN THỊ PHIÊN BẢN (VERSION 2.7)
local VersionLabel = Instance.new("TextLabel")
VersionLabel.Size = UDim2.new(0, 160, 0, 25)
VersionLabel.Position = UDim2.new(0, 10, 0.35, -30)
VersionLabel.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
VersionLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
VersionLabel.Text = "📌 TSB Tool v2.7"
VersionLabel.Font = Enum.Font.GothamBold
VersionLabel.TextSize = 13
VersionLabel.Active = true
VersionLabel.Draggable = true
VersionLabel.Parent = ScreenGui
Instance.new("UICorner", VersionLabel).CornerRadius = UDim.new(0, 8)

-- NÚT 1: ANTI-LAG
local AntiLagBtn = Instance.new("TextButton")
AntiLagBtn.Size = UDim2.new(0, 160, 0, 35)
AntiLagBtn.Position = UDim2.new(0, 10, 0.35, 0)
AntiLagBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
AntiLagBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
AntiLagBtn.Text = "💥 Anti-Lag: OFF"
AntiLagBtn.Font = Enum.Font.GothamBold
AntiLagBtn.TextSize = 12
AntiLagBtn.Active = true
AntiLagBtn.Draggable = true
AntiLagBtn.Parent = ScreenGui
Instance.new("UICorner", AntiLagBtn).CornerRadius = UDim.new(0, 8)

-- NÚT 2: FREEZE SPAWN + AUTO DIE
local FreezeAutoDieBtn = Instance.new("TextButton")
FreezeAutoDieBtn.Size = UDim2.new(0, 160, 0, 35)
FreezeAutoDieBtn.Position = UDim2.new(0, 10, 0.35, 40)
FreezeAutoDieBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
FreezeAutoDieBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FreezeAutoDieBtn.Text = "💀 Freeze + Auto Die: OFF"
FreezeAutoDieBtn.Font = Enum.Font.GothamBold
FreezeAutoDieBtn.TextSize = 11
FreezeAutoDieBtn.Active = true
FreezeAutoDieBtn.Draggable = true
FreezeAutoDieBtn.Parent = ScreenGui
Instance.new("UICorner", FreezeAutoDieBtn).CornerRadius = UDim.new(0, 8)

-- NÚT 3: FREEZE PURE
local FreezeOnlyBtn = Instance.new("TextButton")
FreezeOnlyBtn.Size = UDim2.new(0, 160, 0, 35)
FreezeOnlyBtn.Position = UDim2.new(0, 10, 0.35, 80)
FreezeOnlyBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
FreezeOnlyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FreezeOnlyBtn.Text = "🧊 Freeze Pure: OFF"
FreezeOnlyBtn.Font = Enum.Font.GothamBold
FreezeOnlyBtn.TextSize = 12
FreezeOnlyBtn.Active = true
FreezeOnlyBtn.Draggable = true
FreezeOnlyBtn.Parent = ScreenGui
Instance.new("UICorner", FreezeOnlyBtn).CornerRadius = UDim.new(0, 8)

-- NÚT 4: AUTO SKILL 1-2-3 (CHECK BẤT TỬ)
local AutoSkillBtn = Instance.new("TextButton")
AutoSkillBtn.Size = UDim2.new(0, 160, 0, 35)
AutoSkillBtn.Position = UDim2.new(0, 10, 0.35, 120)
AutoSkillBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
AutoSkillBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
AutoSkillBtn.Text = "⚔️ Auto Skill 1-2-3: OFF"
AutoSkillBtn.Font = Enum.Font.GothamBold
AutoSkillBtn.TextSize = 11
AutoSkillBtn.Active = true
AutoSkillBtn.Draggable = true
AutoSkillBtn.Parent = ScreenGui
Instance.new("UICorner", AutoSkillBtn).CornerRadius = UDim.new(0, 8)

---------------------------------------------------------
-- 2. LOGIC ANTI-LAG & CLEAR CLOUDS
---------------------------------------------------------
local antiLagEnabled = false
local connections = {}
local hiddenObjects = {}

local function removeAllClouds()
    pcall(function()
        for _, v in ipairs(Lighting:GetChildren()) do
            if v:IsA("Clouds") or v:IsA("Sky") then v:Destroy() end
        end
        for _, v in ipairs(Workspace:GetDescendants()) do
            if v:IsA("Clouds") or (v:IsA("BasePart") and string.find(string.lower(v.Name), "cloud")) then
                v:Destroy()
            end
        end
    end)
end

local function isCharacter(obj)
    if not obj then return false end
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr.Character and (obj == plr.Character or obj:IsDescendantOf(plr.Character)) then
            return true
        end
    end
    return false
end

local function createAntiVoidFloor()
    if not Workspace:FindFirstChild("AntiVoidSafetyFloor") then
        local floor = Instance.new("Part")
        floor.Name = "AntiVoidSafetyFloor"
        floor.Size = Vector3.new(200000, 20, 200000)
        floor.Position = Vector3.new(0, -20, 0)
        floor.Anchored = true
        floor.CanCollide = true
        floor.Material = Enum.Material.SmoothPlastic
        floor.Transparency = 1
        floor.Parent = Workspace
    end
end

local function processObject(obj)
    if not obj or not antiLagEnabled then return end
    pcall(function()
        if obj.Name == "AntiVoidSafetyFloor" or obj:IsA("Camera") or obj:IsA("Terrain") then return end
        if obj:IsA("Clouds") or (obj:IsA("BasePart") and string.find(string.lower(obj.Name), "cloud")) then
            obj:Destroy()
            return
        end
        if isCharacter(obj) then return end
        if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") 
            or obj:IsA("Fire") or obj:IsA("Sparkles") or obj:IsA("Highlight") 
            or obj:IsA("Beam") or obj:IsA("Decal") or obj:IsA("Texture") then
            obj:Destroy()
            return
        end
        if obj:IsA("BasePart") or obj:IsA("MeshPart") then
            if not hiddenObjects[obj] then
                hiddenObjects[obj] = {
                    Transparency = obj.Transparency,
                    Material = obj.Material,
                    CastShadow = obj.CastShadow,
                    CanCollide = obj.CanCollide
                }
            end
            obj.Material = Enum.Material.SmoothPlastic
            obj.Reflectance = 0
            obj.CastShadow = false
            obj.Transparency = 1
            obj.CanCollide = false
            if obj:IsA("MeshPart") then obj.RenderFidelity = Enum.RenderFidelity.Performance end
        end
    end)
end

local function enableAntiLag()
    antiLagEnabled = true
    createAntiVoidFloor()
    removeAllClouds()
    pcall(function()
        Lighting.GlobalShadows = false
        Lighting.FogEnd = 9e9
        Lighting.Brightness = 1
        Lighting.ClockTime = 12
        for _, v in ipairs(Lighting:GetChildren()) do
            if v:IsA("Sky") or v:IsA("Clouds") or v:IsA("PostEffect") or v:IsA("Atmosphere") then v:Destroy() end
        end
        if Workspace.Terrain then Workspace.Terrain:Clear() end
    end)
    for _, v in ipairs(Workspace:GetDescendants()) do processObject(v) end
    local conn = Workspace.DescendantAdded:Connect(function(child)
        if antiLagEnabled then task.wait(0.02) processObject(child) end
    end)
    table.insert(connections, conn)
end

local function disableAntiLag()
    antiLagEnabled = false
    for _, conn in ipairs(connections) do conn:Disconnect() end
    connections = {}
    for obj, data in pairs(hiddenObjects) do
        if obj and obj.Parent then
            pcall(function()
                obj.Transparency = data.Transparency
                obj.Material = data.Material
                obj.CastShadow = data.CastShadow
                obj.CanCollide = data.CanCollide
            end)
        end
    end
    hiddenObjects = {}
    local floor = Workspace:FindFirstChild("AntiVoidSafetyFloor")
    if floor then floor:Destroy() end
end

AntiLagBtn.MouseButton1Click:Connect(function()
    if not antiLagEnabled then
        enableAntiLag()
        AntiLagBtn.Text = "⚡ Anti-Lag: ON"
        AntiLagBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
    else
        disableAntiLag()
        AntiLagBtn.Text = "💥 Anti-Lag: OFF"
        AntiLagBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
    end
end)

---------------------------------------------------------
-- 3. TỌA ĐỘ & FREEZE LOGIC
---------------------------------------------------------
local targetCFrame1 = CFrame.new(
    439.180542, 439.511902, -375.309557,
    0.736803472, -4.96859727e-08, 0.676106989,
    -3.74575819e-08, 1, 1.14308612e-07,
    -0.676106989, -1.09542017e-07, 0.736803472
)

local targetCFrame2 = CFrame.new(
    437.820496, 439.511902, -376.999542,
    -0.741123915, 2.3805649e-08, -0.671368241,
    1.33074112e-08, 1, 2.07683453e-08,
    0.671368241, 6.45233154e-09, -0.741123915
)

-- 3A. FREEZE AUTO DIE
local isAutoDieFrozen = false
local autoDieConnection = nil
local hitDetector = nil
local isDying = false

local function triggerKillCreditDeath()
    if isDying then return end
    isDying = true
    if autoDieConnection then autoDieConnection:Disconnect() autoDieConnection = nil end
    task.wait(0.02)
    local char = LocalPlayer.Character
    if char then
        pcall(function()
            if char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart:Destroy() else char:BreakJoints() end
        end)
    end
end

local function setupHitDetection(char)
    if not char then return end
    local hum = char:WaitForChild("Humanoid", 3)
    if not hum then return end
    if hitDetector then hitDetector:Disconnect() end
    local currentHp = hum.Health
    hitDetector = hum.HealthChanged:Connect(function(newHp)
        if isAutoDieFrozen and newHp < currentHp and not isDying then triggerKillCreditDeath() end
        currentHp = newHp
    end)
    char.ChildAdded:Connect(function(child)
        if isAutoDieFrozen and not isDying and (child.Name == "Ragdoll" or child.Name == "Knockback" or child.Name == "Stun") then
            triggerKillCreditDeath()
        end
    end)
end

FreezeAutoDieBtn.MouseButton1Click:Connect(function()
    isAutoDieFrozen = not isAutoDieFrozen
    if isAutoDieFrozen then
        FreezeAutoDieBtn.Text = "💀 Freeze + Auto Die: ON"
        FreezeAutoDieBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
        isDying = false
        setupHitDetection(LocalPlayer.Character)
        if autoDieConnection then autoDieConnection:Disconnect() end
        autoDieConnection = RunService.RenderStepped:Connect(function()
            if not isDying then
                local char = LocalPlayer.Character
                if char and char:FindFirstChild("HumanoidRootPart") then
                    local hrp = char.HumanoidRootPart
                    hrp.CFrame = targetCFrame1
                    hrp.AssemblyLinearVelocity = Vector3.zero
                    hrp.AssemblyAngularVelocity = Vector3.zero
                end
            end
        end)
    else
        FreezeAutoDieBtn.Text = "💀 Freeze + Auto Die: OFF"
        FreezeAutoDieBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
        isDying = false
        if autoDieConnection then autoDieConnection:Disconnect() autoDieConnection = nil end
        if hitDetector then hitDetector:Disconnect() hitDetector = nil end
    end
end)

-- 3B. FREEZE PURE
local isPureFrozen = false
local pureFreezeConnection = nil

FreezeOnlyBtn.MouseButton1Click:Connect(function()
    isPureFrozen = not isPureFrozen
    if isPureFrozen then
        FreezeOnlyBtn.Text = "🧊 Freeze Pure: ON"
        FreezeOnlyBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
        if pureFreezeConnection then pureFreezeConnection:Disconnect() end
        pureFreezeConnection = RunService.RenderStepped:Connect(function()
            local char = LocalPlayer.Character
            if char and char:FindFirstChild("HumanoidRootPart") then
                local hrp = char.HumanoidRootPart
                hrp.CFrame = targetCFrame2
                hrp.AssemblyLinearVelocity = Vector3.zero
                hrp.AssemblyAngularVelocity = Vector3.zero
            end
        end)
    else
        FreezeOnlyBtn.Text = "🧊 Freeze Pure: OFF"
        FreezeOnlyBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
        if pureFreezeConnection then pureFreezeConnection:Disconnect() pureFreezeConnection = nil end
    end
end)

---------------------------------------------------------
-- 4. LOGIC AUTO SKILL 1 - 2 - 3 (CHECK BẤT TỬ & KHOẢNG CÁCH)
---------------------------------------------------------
local autoSkillEnabled = false
local autoSkillThread = nil

-- Hàm giả lập ấn phím
local function pressKey(keyCode)
    VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
    task.wait(0.05)
    VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
end

-- Hàm kiểm tra xem Target có đang trong trạng thái BẤT TỬ / SPAWN PROTECTION không
local function isTargetInvulnerable(targetChar)
    if not targetChar then return true end
    
    -- Check các hiệu ứng / tag bất tử quen thuộc của Roblox & TSB
    if targetChar:FindFirstChildOfClass("ForceField") then return true end
    if targetChar:FindFirstChild("SpawnProtection") or targetChar:FindFirstChild("Invulnerable") or targetChar:FindFirstChild("Protection") then return true end
    
    -- Check Highlight dạng Khiên Bất Tử
    for _, v in ipairs(targetChar:GetChildren()) do
        if v:IsA("Highlight") and (string.find(string.lower(v.Name), "shield") or string.find(string.lower(v.Name), "spawn")) then
            return true
        end
    end
    
    return false
end

-- Hàm tìm đối thủ hợp lệ ở gần (Dưới 25 studs & ĐÃ MẤT BẤT TỬ)
local function getValidTargetNear()
    local myChar = LocalPlayer.Character
    if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then return nil end
    local myPos = myChar.HumanoidRootPart.Position

    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") and plr.Character:FindFirstChild("Humanoid") then
            local targetChar = plr.Character
            local targetHum = targetChar.Humanoid
            
            -- Chỉ target người còn sống
            if targetHum.Health > 0 then
                local dist = (targetChar.HumanoidRootPart.Position - myPos).Magnitude
                
                -- Khoảng cách sát người (25 studs)
                if dist <= 25 then
                    -- KIỂM TRA ĐÃ MẤT BẤT TỬ CHƯA?
                    if not isTargetInvulnerable(targetChar) then
                        return targetChar
                    end
                end
            end
        end
    end
    return nil
end

-- Vòng lặp tự động tung chiêu 1 -> 2 -> 3
local function startAutoSkillLoop()
    autoSkillThread = task.spawn(function()
        while autoSkillEnabled do
            task.wait(0.1)
            
            local validTarget = getValidTargetNear()
            if validTarget then
                -- Đã phát hiện đối thủ ở gần & HẾT BẤT TỬ -> Xả Combo Skill 1, 2, 3
                pressKey(Enum.KeyCode.One)
                task.wait(0.3)
                
                pressKey(Enum.KeyCode.Two)
                task.wait(0.3)
                
                pressKey(Enum.KeyCode.Three)
                task.wait(0.5)
            end
        end
    end)
end

AutoSkillBtn.MouseButton1Click:Connect(function()
    autoSkillEnabled = not autoSkillEnabled
    
    if autoSkillEnabled then
        AutoSkillBtn.Text = "⚔️ Auto Skill 1-2-3: ON"
        AutoSkillBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
        startAutoSkillLoop()
    else
        AutoSkillBtn.Text = "⚔️ Auto Skill 1-2-3: OFF"
        AutoSkillBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
        if autoSkillThread then
            task.cancel(autoSkillThread)
            autoSkillThread = nil
        end
    end
end)

---------------------------------------------------------
-- TỰ ĐỘNG BẬT LẠI KHI HỒI SINH
---------------------------------------------------------
LocalPlayer.CharacterAdded:Connect(function(char)
    isDying = false
    
    if isAutoDieFrozen then
        task.wait(0.05)
        setupHitDetection(char)
        if not autoDieConnection then
            autoDieConnection = RunService.RenderStepped:Connect(function()
                if not isDying then
                    local curChar = LocalPlayer.Character
                    if curChar and curChar:FindFirstChild("HumanoidRootPart") then
                        local hrp = curChar.HumanoidRootPart
                        hrp.CFrame = targetCFrame1
                        hrp.AssemblyLinearVelocity = Vector3.zero
                        hrp.AssemblyAngularVelocity = Vector3.zero
                    end
                end
            end)
        end
    end
    
    if isPureFrozen then
        if not pureFreezeConnection then
            pureFreezeConnection = RunService.RenderStepped:Connect(function()
                local curChar = LocalPlayer.Character
                if curChar and curChar:FindFirstChild("HumanoidRootPart") then
                    local hrp = curChar.HumanoidRootPart
                    hrp.CFrame = targetCFrame2
                    hrp.AssemblyLinearVelocity = Vector3.zero
                    hrp.AssemblyAngularVelocity = Vector3.zero
                end
            end)
        end
    end
end)
